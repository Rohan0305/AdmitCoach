type Experience = {
  role: string;
  organization: string;
  startDate: string;
  endDate: string;
  description: string;
};

type User = {
  firstName: string;
  lastName: string;
  email: string;
};
